<script lang="ts">
    import { Modal, modalStore } from "$lib/modals/modalStore";
    // import {
    //     Dialog,
    //     DialogDescription,
    //     DialogOverlay,
    //     DialogTitle,
    // } from "@rgossiaux/svelte-headlessui";
</script>
<!-- 
<Dialog open={$modalStore.backend_auth} class="modal_root">
    <DialogOverlay class="modal_overlay" />
    <div class="modal_card">
        <div class="text-left gap-8 p-14">
            <DialogTitle
                as="h3"
                class="text-xl font-semibold leading-6 text-gray-900"
            >
                {`Verifying your account...`}
            </DialogTitle>
            <div class="mt-2">
                <DialogDescription as="p" class="text-gray-500 text-lg">
                    {`In order to do things like post comments, you need to authenticate with OpenPredict by signing a random string.\nDon't worry, you'll only have to do this once per session.`}
                </DialogDescription>
            </div>
        </div>
    </div>
</Dialog> -->
